## Lens [(OpenLens)](https://github.com/MuhammedKalkan/OpenLens)

### Troubleshooting

- Problem: Cannot connect to cluster
  - Check awscli version on Windows host, update if necessary (see `awscli.md`).
    - `aws --version`
  - Check AWS credentials on Windows host
    - `aws configure`
